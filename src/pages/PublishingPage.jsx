import { useMemo, useState } from 'react';
import PageHeader from '../components/PageHeader';
import { useWorkspace } from '../state/WorkspaceContext';
export default function PublishingPage(){const{projects}=useWorkspace();const[projectId,setProjectId]=useState(projects[0]?.id);const[title,setTitle]=useState(projects[0]?.title||'');const[category,setCategory]=useState('Film');const[featured,setFeatured]=useState(true);const p=useMemo(()=>projects.find(x=>x.id===projectId),[projects,projectId]);const choose=(id)=>{setProjectId(id);const q=projects.find(x=>x.id===id);setTitle(q?.title||'')};return <div className="page"><PageHeader eyebrow="OUTPUT / 01" title="Publishing" copy="Turn completed production records into clean public-facing work without rewriting the project from scratch."/><div className="publishing-layout"><form className="publish-form"><label>Project<select value={projectId} onChange={e=>choose(e.target.value)}>{projects.map(x=><option key={x.id} value={x.id}>{x.title}</option>)}</select></label><label>Public title<input value={title} onChange={e=>setTitle(e.target.value)}/></label><label>Category<select value={category} onChange={e=>setCategory(e.target.value)}><option>Film</option><option>Documentary</option><option>Studio</option><option>Motion</option></select></label><label className="check"><input type="checkbox" checked={featured} onChange={e=>setFeatured(e.target.checked)}/> Feature on homepage</label><button type="button" className="primary-button">Save publishing draft</button></form><div className="publish-preview"><span className="eyebrow">PUBLIC PREVIEW</span><div className="publish-preview__media"><video muted autoPlay loop playsInline src="/media/kadri-review.mp4"/></div><span>{category} / {p?.location}</span><h2>{title}</h2><p>{p?.brief}</p>{featured&&<b className="feature-flag">FEATURED</b>}</div></div></div>}
